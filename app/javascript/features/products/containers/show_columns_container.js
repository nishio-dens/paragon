import { connect } from 'react-redux'
import ShowColumns from '../components/show_columns'

const mapStateToProps = state => {
  return {
  }
}

const mapDispatchToProps = dispatch => ({
})

export default connect(mapStateToProps, mapDispatchToProps)(ShowColumns)